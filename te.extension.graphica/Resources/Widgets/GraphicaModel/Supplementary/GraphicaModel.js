(function ($) {
    if (typeof $.graphica === 'undefined') { $.graphica = {}; }
    if (typeof $.graphica.model === 'undefined') { $.graphica.model = {};}

    var api = {
        register: function (context) { }
    };

    $.graphica.model = api;
})(jQuery);
